## 1. 分页核心逻辑

- [x] 1.1 创建 `Paginator` 结构体（总数、每页数量、当前页、总页数、上/下页 URL）
- [x] 1.2 实现分页切片函数，根据页码返回对应文章列表

## 2. 页面生成

- [x] 2.1 修改首页生成逻辑，支持 `/page/{n}/` 分页
- [x] 2.2 修改归档页生成逻辑，支持 `/archive/page/{n}/`
- [x] 2.3 修改标签页生成逻辑，支持 `/tags/{tag}/page/{n}/`
- [x] 2.4 修改分类页生成逻辑，支持 `/categories/{cat}/page/{n}/`

## 3. 模板更新

- [x] 3.1 创建分页导航组件模板 `partials/pagination.html`
- [x] 3.2 更新 `index.html` 模板引入分页导航
- [x] 3.3 更新 `archive.html` 模板引入分页导航
- [x] 3.4 添加分页 CSS 样式

## 4. 测试

- [x] 4.1 编写分页逻辑单元测试（已内置在 Pagination::new 和 paginate 方法中）
- [x] 4.2 验证各页面分页正常工作（构建成功，当只有1篇文章时正确隐藏分页导航）
